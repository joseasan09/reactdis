import CartWidget from "../CartWidget/CartWidget"

const ItemListContainer = ({ greeting }) => {
    
    return (
        <div class="p-3 mb-2 bg-primary text-white border border-5 rounded-pill">{greeting}
        <dispenser/>
        </div>

        
    )
}

export default ItemListContainer 